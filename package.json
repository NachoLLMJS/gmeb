{
  "name": "gmeb-reddit-claim-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "cross-env NODE_ENV=test node --test",
    "check": "node --check server.js && node --check public/app.js && cross-env NODE_ENV=test node --test"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}